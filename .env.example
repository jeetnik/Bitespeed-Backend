# Get your PostgreSQL connection string from Neon (https://neon.tech) or any other provider
DATABASE_URL="postgresql://username:password@hostname/database?sslmode=require"

# Server port (optional, defaults to 3000)
PORT=3000
